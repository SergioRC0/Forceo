'use client';

export default function LoginButton() {
  return (
    <button
        type='submit'
        className="w-full px-4 py-2 text-white bg-black btn-hover"
    >
        Iniciar sesión
    </button>
    );
}
